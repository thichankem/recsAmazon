# Experiment and benchmark execution configuration

experiment:
  name: "exp_production_002"
  description: "Production API endpoint benchmark run"
  seed: 42
  num_runs: 5
  output_dir: "experiments/exp_production_002"
  save_raw_responses: true
  enable_profiler: false
